# Schedules

Recurring runs via cron. Create a schedule with a goal + cron expression; the scheduler triggers it automatically (5-hour usage windows still apply). Coming to the worker service: pause/resume from the UI.
